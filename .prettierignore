node_modules
dist
build
.turbo
.expo
coverage
pnpm-lock.yaml
tools/project-graph/output

# Storybook build output
storybook-static/

# Generated by NativeWind; it rewrites the file and Prettier would fight it.
nativewind-env.d.ts

# Agent worktrees — full checkouts of this repository under .claude/. The
# patterns above are anchored to this file's directory, so `tools/project-graph/
# output` does NOT match the same path inside a worktree; without this line a
# root `pnpm format` reaches into another agent's tree and rewrites its
# generated graph output underneath it.
.claude/
